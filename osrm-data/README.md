# osrm-data/

Local OpenStreetMap extracts and prepared OSRM graph files live here.
**Do not commit these files** — they are large and gitignored.

## Contents after setup

```text
osrm-data/
  README.md                 # this file (committed)
  <map>.osm.pbf             # downloaded extract (ignored)
  <map>.osrm.*              # prepared graph files (ignored)
```

See the project README → **OSRM map data** for download, prepare, and cleanup steps.
